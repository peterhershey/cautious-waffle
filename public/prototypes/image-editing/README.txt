Image-editing prototype — drop your exports here.

Needed (PNG or JPG, square-ish, ~1080px+):

  variant-1.png   top-left grid tile     — futuristic car, flowery mountain meadow
  variant-2.png   top-right grid tile    — silver car, glowing wheels, waterfall  ← the selectable one
  variant-3.png   bottom-left grid tile  — blue car, autumn road
  variant-4.png   bottom-right grid tile — silver car, mountain pass

  hero.png        full-res of variant-2  — this is the image the canvas zooms into,
                                           so export it as large/sharp as you can
                                           (the region zoom is a crop of this).

Filenames matter — the prototype reads these exact names. If you'd rather use
different names, tell me and I'll update imageEditingData.ts.
